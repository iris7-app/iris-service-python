apiVersion: v1
kind: Service
metadata:
  name: iris-service-python
  namespace: mirador
  labels:
    app.kubernetes.io/name: iris-service-python
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: mirador
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: iris-service-python
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
